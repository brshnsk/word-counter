
export function Counter({lettersCounter, wordsCounter}) {
  return (
    <h2>
      Hay {lettersCounter} Caracteres y {wordsCounter} palabras en este texto.
    </h2>
  );
}


export default Counter;